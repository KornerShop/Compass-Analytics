machine:
  node:
    version: 7.10.0

dependencies:
  override:
    - yarn
  cache_directories:
    - ~/.cache/yarn
  pre:
    - yarn global add now
    
deployment:
  production:
    branch: production
    commands:
      - now -t ${NOW_TOKEN}
      - now -t ${NOW_TOKEN} alias
