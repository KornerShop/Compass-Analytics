machine:
  node:
    version: 7.10.0

dependencies:
  override:
    - yarn
  cache_directories:
    - ~/.cache/yarn
  pre:
    - yarn global add now

test:
  override:
    - yarn test

deployment:
  production:
    branch: master
    commands:
      - now --public -t ${NOW_TOKEN}
      - now -t ${NOW_TOKEN} alias compass-analytics
